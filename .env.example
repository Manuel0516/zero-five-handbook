# Copy to .env (locally) or ~/services/zero-five-handbook/.env (VPS) and fill in.
# Used by the AI search endpoint (/api/chat).
OPENROUTER_API_KEY=
# Optional — defaults to deepseek/deepseek-v4-flash in src/app/api/chat/route.ts
# OPENROUTER_MODEL=
